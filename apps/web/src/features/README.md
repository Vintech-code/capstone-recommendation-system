# Feature modules

Create one folder per approved business feature under this directory. Keep
feature-specific components in `src/features/<feature>/components` and promote a
component to `src/components/shared` only when multiple approved features reuse
it.

Do not add a feature module while its institutional dependencies remain open.
